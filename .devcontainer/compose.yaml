services:
  devcontainer:
    build: ./.devcontainer
    command: sleep infinity
    volumes:
      - type: bind
        source: .
        target: /workspaces/hugo-site
        consistency: cached
    init: true
